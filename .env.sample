# Twin Configuration

# Audio settings
SAMPLE_RATE=16000
BUFFER_DURATION=3
SMALL_BUFFER_DURATION=0.2
CHANNELS=1
CHUNK_SIZE=1024
SILENCE_THRESHOLD=0.0005

# Transcription settings
LANGUAGE=en
SIMILARITY_THRESHOLD=85
WHISPER_MODEL=turbo

# Inference and command settings
RISK_THRESHOLD=0.5
COOLDOWN_PERIOD=0
HISTORY_BUFFER_SIZE=4
HISTORY_MAX_CHARS=4000
HISTORY_INCLUDE_CHUNKS=6

# Vector search thresholds
AMY_DISTANCE_THRESHOLD=1.1
NA_DISTANCE_THRESHOLD=1.4
HIP_DISTANCE_THRESHOLD=1.1

# Wake/sleep settings
WAKE_TIMEOUT=24
WAKE_SOUND_FILE=/media/mass/scripts/twin/wake.wav
SLEEP_SOUND_FILE=/media/mass/scripts/twin/sleep.wav

# TTS settings
TTS_PYTHON_PATH=/home/andy/venvs/tts-env/bin/python
TTS_SCRIPT_PATH=/home/andy/scripts/tts/tts.py

# Remote services
# REMOTE_STORE_URL=http://example.com/store
# REMOTE_INFERENCE_URL=http://example.com/inference
# REMOTE_TRANSCRIBE_URL=http://example.com/transcribe

# Compute settings (optional, auto-detected if not set)
# DEVICE_TYPE=cuda
# COMPUTE_TYPE=float16

# Reports and logs
QC_REPORT_DIR=reports
GENERAL_REPORT_FILE=general_report.txt
LOG_FILE=logs/continuous.log 